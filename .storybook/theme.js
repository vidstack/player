import { create } from '@web/storybook-prebuilt/theming';

export default create({
  base: 'dark',
  brandTitle: 'Vidstack Player',
  brandUrl: 'https://vidstack.io',
  brandImage: '.storybook/storybook.png',
});
