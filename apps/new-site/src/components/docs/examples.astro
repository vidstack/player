---
import MockCodeEditor from '../mocks/code-editor/mock-code-editor.astro';

const path = Astro.url.pathname;

// PATH+JSLIB
// snippets = get(codeSnippets).filter((snippet) => snippet.id === id),

// { path: 'app/page.tsx' (rea), snippet: 'home/editor/page' },
// examples/(relative to path)

// openFile??
---

<MockCodeEditor
  files={[{ path: 'app/player/player.tsx', snippet: 'home/editor/player' }]}
  openFile="app/player/player.tsx"
>
  <slot />
</MockCodeEditor>
