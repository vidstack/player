---
import NavbarDesktop from '../navbar/navbar-desktop.astro';
import NavbarMobile from '../navbar/navbar-mobile.astro';

interface Props {
  announcementBar?: boolean;
}

const { announcementBar = false } = Astro.props;
---

<NavbarDesktop {announcementBar} />
<NavbarMobile {announcementBar} />
