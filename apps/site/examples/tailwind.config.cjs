// When the examples are embedded as an `<iframe>` inside the main site it'll search for a
// Tailwind CSS configuration file and error out if not found. This file prevents that.
module.exports = {};
