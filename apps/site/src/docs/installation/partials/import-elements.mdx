import Snippet from './import-elements-snippet.svelte';

The following import will register all custom media elements (e.g., `<media-player>`) and import
global TypeScript types:

<Snippet client:visible />
