/**
 * Indicates the type of media currently loading/loaded.
 */
export type MediaType = string;
export namespace MediaType {
    const Unknown: string;
    const Audio: string;
    const Video: string;
    const LiveVideo: string;
}
