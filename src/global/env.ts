/* c8 ignore next */
export const DEV_MODE = true;

/* c8 ignore start */
if (DEV_MODE) {
  console.warn(
    '`@vidstack/elements` is in dev mode. Not recommended for production!'
  );
}
/* c8 ignore stop */
