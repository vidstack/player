import { LIB_PREFIX } from '../../shared/constants';

export const FAKE_MEDIA_PROVIDER_ELEMENT_TAG_NAME = `fake-media-provider`;

export const VDS_FAKE_MEDIA_PROVIDER_ELEMENT_TAG_NAME = /** @type {`${typeof LIB_PREFIX}-${typeof FAKE_MEDIA_PROVIDER_ELEMENT_TAG_NAME}`} */ (`${LIB_PREFIX}-${FAKE_MEDIA_PROVIDER_ELEMENT_TAG_NAME}`);
